Session 4 summary: Pre-publish review found 6 issues. Critical: pydantic/pyyaml in core deps (demo-only) → moved to [demo] extra; missing LICENSE file (build-breaking) → created MIT; empty wheel (stale root egg-info shadowed src/ layout) → clean rebuild. Also: sentence-transformers moved to [local-embed] optional extra (torch cost); last_user_text() extracted as shared helper (was copied 5×); dead `and m.ctx_limit` guard removed from selector.py. pyproject.toml license field updated to SPDX format. Version bumped to 0.1.1 because broken 0.1.0 wheel was already on PyPI (filename reuse blocked). twine upload of 0.1.1 attempted but not yet confirmed — next session runs `twine upload dist/*` with ~/.pypirc credentials.
