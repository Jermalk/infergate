## Carried over:

Session 7 acted on round 3 ov_server feedback. Two changes shipped in v0.1.4: (1) `Router.reselect(task_class, scope, force_tier, complexity, estimated_tokens) → RouteDecision` — synchronous public method that re-runs model selection with caller-supplied scope/tier overrides, returning `strategy=RESELECT`; enables ov_server to delete `router._select_model()` and the parallel config.json task-class catalogue. (2) `RouteDecision.task_directive: str | None` — carries the matched hashtag directive (e.g. "code") out of `decide()` so callers don't need a second detection pass. `RouteStrategy.RESELECT` enum value added. 8 new tests, 87/87 pass. `addressed_03_v0.1.4.md` written, `SIGNAL.md` flipped to RELEASE READY. Next session opens with `feedback/SIGNAL.md` check.
