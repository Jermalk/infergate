## Carried over:

Session 11 implemented roadmap step 2 (v0.1.7). _EmbedCache private class in router.py using OrderedDict for LRU eviction; maxsize=0 disables caching. RouterSettings.embedding_cache_size: int = 512 added, parsed through from_dict(). Cache lives on Router instance, keyed by query text, stores full (task_class, confidence, embedding) tuple. RouteTrace.cache_hit: bool|None added — False on miss, True on hit, None when embedding path not taken. embedding_ms only measured on cache miss (actual embed call). 8 new tests (LRU eviction, miss/hit, disabled, signal-path None, config field), 112/112 pass. Next: step 3 — decide_batch(); uncached queries grouped into single embed_batch() call, signal-path requests routed individually.
