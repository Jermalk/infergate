## Carried over:

Session 12 implemented roadmap step 3 (v0.1.8). Router.decide_batch(requests, trace=False) -> list[RouteDecision]: three-phase pipeline — (1) per-request signal/keyword detection + cache lookup, collecting unique uncached queries; (2) single embed_batch() call for all uncached unique queries, results stored in cache; (3) per-request scope resolution + model selection. Private _classify_vec() method added for centroid comparison without encoding. Tests verify: identical queries → 1 embed_batch() call; distinct queries → 1 call with N items; signal-path requests skip embed; batch populates cache for subsequent decide(); trace.cache_hit correctly True/False/None. Feature roadmap complete: 1+5 (v0.1.5), 4 (v0.1.6), 2 (v0.1.7), 3 (v0.1.8). 120/120 tests pass.
