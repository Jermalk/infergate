## Carried over:

Session 10 implemented roadmap step 4 (v0.1.6). RouteTrace added: EliminatedCandidate dataclass (model_id, backend, reason: Literal["no_backend","scope","unavailable","ctx_limit","modality"]), RouteTrace dataclass (eliminated list, scope_source str, embedding_ms float|None), RouteDecision.trace field. decide() gains trace=False keyword arg — zero overhead when False. selector.select_model() gains _eliminated param (mutable list); records elimination reasons inline. scope_source captured during scope resolution. embedding_ms timed with perf_counter only when trace=True. 11 new tests, 104/104 pass. Next: step 2 — LRU embedding cache, RouterSettings.embedding_cache_size field; cache lives on Router instance, populated on first decide() per unique query.
